# 系统设置

时间设置中可配置当前设备使用的时区及时间，通过手动方式或NTP同步方式进行配置。

在系统菜单点击“系统>系统设置>时间设置”，进入时间设置页面。

手动配置展示如下：

NTP同步配置展示如下：

时间设置的配置项与详细说明如下：

配置设备自身访问域名所用DNS，支持配置主备DNS服务器，支持检测域名。

在系统菜单点击“系统>系统设置>DNS”，进入DNS配置页面。

DNS的配置项与详细说明如下：

为减轻设备自身的存储压力，用户可以配置SMTP服务器，将日志发送到指定的邮箱。

在系统菜单点击“系统>系统设置>邮件服务器设置”，进入邮件服务器设置配置页面。

告警邮件配置配置项及详细说明如下：

提示：

在“系统-日志设定-日志过滤”中，必须勾选E-mail报警。