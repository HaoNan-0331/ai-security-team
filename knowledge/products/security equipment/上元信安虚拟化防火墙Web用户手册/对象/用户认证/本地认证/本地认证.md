# 本地认证

启用本地认证功能后，用户打开浏览器访问HTTP页面会被定向到本地认证登录页面，输入正确的用户名和密码即可通过认证。

在系统菜单点击“对象>用户认证>本地认证”，进入本地认证配置页面。

本地认证配置项及详细说明如下：

注意：

心跳超时：如果关闭认证登录成功页面，则在设置的超时时间到期后用户需重新认证，不关闭则根据强制重登录间隔计算。

流量超时：用户连续一段时间无流量产生，此段时间超过设置的超时时间，用户需重新认证。