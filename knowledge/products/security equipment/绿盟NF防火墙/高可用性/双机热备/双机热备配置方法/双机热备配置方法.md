# 双机热备配置方法

进入 高可用 > 双机热备 页面，如图7-1所示，页面展示内容如表7-1所示。

双机热备

双机热备页面展示内容

全局配置

配置全局性配置参数后才可以启用高可用性。全局配置包括HA的运行模式、心跳配置、同步策略。

在如图7-1所示的页面中单击【全局设置】按钮，配置全局性参数，参数的详细信息如表7-2所示。

HA全局配置参数

HA同步项

配置同步状态

同步状态功能默认关闭。在基本状态区域，单击“高可用”右侧的开关按钮启用高可用后，可以配置同步状态参数。参数说明如表7-4所示。

高可用功能禁用后，同步状态配置自动关闭。

同步状态参数说明

配置VRRP监控线路

监控线路是指主墙和备墙相互备份，以保证完成数据转发的线路。

单击【添加线路】按钮，即可添加VRRP监控线路，页面如图7-2所示。

添加VRRP监控线路

配置基本信息，参数说明如表7-5所示。

基本信息参数

配置成员接口。

只要主线路的一个成员接口Down，线路中其他接口将释放虚拟IP，同时切换至从线路处理业务。

在“接口”处选择接口类型，可选项有二层、三层、虚拟线，并选择成员接口。

配置成员接口的参数，不同类型的接口需要配置不同的参数，如表7-6所示。

成员接口参数

配置监控接口。

当监听接口处于Down状态，主从线路将进行切换。

在“接口”处选择接口类型，可选项有二层、三层、虚拟线，并选择具体的监控接口。

配置监控接口的权值。当主从线路的所有监控接口状态均为Down，主线路切换到接口权值和最大的线路，从线路切换到权值小的线路。取值范围为0～254的整数。

配置联动接口。

当VRRP的主从线路切换时，该类接口的状态会被切换，其中主线路为Up，从线路状态为Down。联动接口必须是VRRP的非成员接口。

在“接口”处选择接口类型，可选项有二层、三层、虚拟线，并选择具体的监控接口。

单击【确定】按钮，完成监控线路新建。

----结束

链路探测

NF使用链路探测技术来判断自身到目的IP地址间的链路是否畅通。支持根据链路探测的结果进行主备切换。有关链路探测的具体配置，请参见 链路探测。

查看运行状态

在运行状态区域，可以查看实现双机热备功能的2台设备的运行状态。

关于基本状态和同步状态的其他参数说明，请参见表7-2。