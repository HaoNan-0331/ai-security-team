# 子网

通过子网掩码的方式指定属于同一网段的IP地址。

进入 策略 > 对象 > 网络 > 子网 页面，子网对象列表显示NF系统中所有的子网对象，包括系统预定义子网对象和自定义的子网对象，any为系统预定义的子网对象。

单击【新建】按钮，配置子网对象参数。单击【确定】按钮，保存子网对象。单击【应用配置】按钮，使对象生效。子网对象参数说明如表6-22所示。

新建对象后，可以对其进行查询、编辑、导出、导入、删除和清空等操作。

子网对象参数