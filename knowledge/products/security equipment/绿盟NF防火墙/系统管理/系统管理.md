# 系统管理

除 认证设置 和 日志存储 模块外，本功能仅支持系统管理员及系统管理员子帐号操作。不同角色帐号操作权限详情见表1-1、表1-2和表1-3。

本章介绍了系统维护的常用操作内容和方法，具体内容如下：

帐号管理功能用于查看和管理系统帐号以及帐号权限。

对登录帐号权限的控制，是通过为帐号赋予一定的角色来实现的。在一个角色中定义了允许帐号执行的系统功能，例如：定义帐号所属的角色，实现允许帐号配置A功能，或禁止帐号配置B功能。

只有weboper、webpolicy和webaudit才有新建角色、新建帐号的权限。其他角色的帐号只能编辑自身帐号的信息。

此处以weboper为例介绍如何配置角色及其帐号。

进入 系统 > 帐号管理 > 帐号信息 页面，可以查看帐号信息、配置角色、配置帐号信息、启用/禁用帐号，如图3-1所示。

帐号信息（weboper）

启用/禁用帐号

在如图3-1所示的页面中，管理员可以启用或禁用该帐号创建的帐号信息。webaudit帐号默认为禁用状态，需要weboper登录后进行启用。启用webaudit帐号后，无法禁用。

只需单击状态栏的开关按钮，即可启用该帐号。对于webaudit帐号，还会弹出初始化密码设置页面，需要输入并确认密码后才可启用webaudit帐号。webpolicy帐号开启与webaudit的操作相同。

查看帐号信息

在“帐号信息”区域，可以查看当前登录的帐号，以及该帐号创建的所有帐号的信息。

在“角色信息”区域，可以查看当前登录帐号所属的角色，以及该帐号创建的所有角色的信息。

配置角色

角色用于定义允许/禁止帐号操作某些菜单的权限。

系统预定义了3种角色：系统管理员、安全策略管理员和安全审计员，分别对应于默认帐号weboper、webpolicy和webaudit。以weboper、webpolicy或webaudit帐号登录设备后，可以对角色进行配置：

新建角色：基于当前登录帐号的角色，创建新角色。新配置的角色的权限必须小于等于当前角色。

编辑角色：只能编辑当前帐号配置的角色。

删除角色：只能删除当前帐号配置的角色。

此处仅以weboper帐号为例介绍如何新建角色。

在如图3-1所示的页面中，单击【新建角色】按钮，配置角色参数，即可新建一个角色。角色参数说明如表3-1所示。

角色参数

配置帐号

weboper、webpolicy和webaudit登录设备后，可以对帐号进行配置：

新建帐号：基于当前登录的帐号创建的角色，创建新的帐号。

编辑帐号：只能编辑当前帐号的部分参数，以及该帐号创建的帐号的所有信息。

删除帐号：只能删除当前帐号创建的帐号。

在如图3-1所示的页面中，单击【新建帐号】按钮，配置帐号参数，即可新建一个帐号。帐号参数说明如表3-2所示。

新建帐号后，可以对其进行查询、编辑和删除等操作。

帐号参数

进入 系统 > 系统 > 帐号管理 > 根系统帐号信息 页面，可以查询、新建根系统帐号以及新建角色，如图3-2所示。根系统帐号支持的操作和管理动作与系统帐号相同，有关新建、管理帐号以及角色管理，参见 帐号信息。

根系统帐号信息

进入 系统 > 系统 > 帐号管理 > 虚拟系统帐号信息 页面，查询和查看虚拟系统帐号信息，如图3-3所示。

虚拟系统帐号在新建虚拟系统时自动生成，不支持新建和删除，仅支持编辑部分参数。参数说明如表3-3所示。

虚拟系统帐号信息

虚拟系统帐号参数

通过认证设置，可以配置系统登录认证相关的参数。

进入 系统 > 帐号管理 > 认证设置 页面，单击【编辑】，配置登录认证参数，参数说明如表3-4所示。

认证参数

该功能仅支持安全策略管理员帐号操作。

进入 系统 ＞ 系统 ＞ 日志存储 页面，配置日志存储相关参数，参数说明如表3-5所示。

日志存储参数

介绍如何查看设备的版本信息，以及在线和离线状态下对系统进行升级的方法。

进入 系统 > 系统升级 页面，如图3-4所示。

系统升级

查看系统版本

在“版本信息”区域，可以查看系统的引擎版本信息以及各类特征库、规则库的版本信息。单击，即可查看到当前是否存在可升级版本。

离线升级

若NF设备无法访问绿盟科技的官方网站，用户可以通过导入升级文件对设备进行升级。

离线升级的操作如下：

在如图3-4所示的页面中，在“升级包类型”右侧的下拉列表框中选择升级文件的类型，升级文件类型说明如表3-6所示。

升级文件类型

将升级文件拖动至右侧的文件上传区域，即可立即升级设备。

----结束

在线升级

若NF设备可以访问绿盟科技的官方网站，用户可以通过在线升级方式升级设备，包括定时升级和手动立即升级2种方式。

定时升级：启用后，系统自动检测最新升级包，在指定时间自动升级所有文件，无需手动干预。

立即升级：立即对设备进行升级。

在如图3-4所示的页面中，配置在线升级的操作如下：

设置升级地址。

升级地址，是指系统升级包的下载地址，默认为绿盟科技的官方网站update.nsfocus.com。填写时注意，不能填写http://开头的URL地址。

设置升级时间。

单击【定时】，设置升级方式和升级时间，如图3-5所示。在线定时升级参数说明如表3-7所示。

单击【立即】，设置升级包类型，如图3-6所示。

在线升级（定时升级）

在线升级（立即升级）

在线定时升级参数

单击【提交】按钮，保存配置。

----结束

该功能仅支持系统管理员帐号操作。

NF的证书分为试用证书和正式证书2类：

试用证书过期后，将无法继续使用NF。

正式证书过期后，可以继续使用NF，但无法进行系统升级。

进入 系统 > 证书管理 页面，如图3-7所示。

证书管理

查看证书详情

通过查看证书详情，用户可以了解NF产品支持的功能模块及其限用日期等信息。

在页面左侧可以查看证书状态信息，信息说明如表3-8所示。

证书状态信息

上传证书

有2种情况下需要导入证书：

初次登录系统时，必须导入证书，否则无法使用NF。

证书过期后需要续期，或者新购功能模块时，需要导入新的证书来更换证书。

导入证书的操作如下：

进入 系统 > 证书管理 页面，如图3-7所示。

单击【证书上传】按钮，弹出选择文件对话框，选择证书文件（*.lic），弹出确认证书信息对话框。

确认导入证书。

证书状态正确，单击【确认导入】按钮，完成导入证书任务，证书即刻生效。

证书状态不正确，单击【取消】按钮，重新执行 步骤 2。

----结束

下载证书

在如图3-7所示的页面中，单击【证书下载】按钮，即可将当前证书下载到本地进行备份。

介绍系统内置各种维护工具的使用方法。

仅X86的海光平台NF设备显示该菜单项。

进入 系统 > 维护工具 > 系统信息 页面，查看系统的内存大小、硬盘大小、内核版本、CPU信息。

进入 系统 > 维护工具 > 网络诊断 页面，可以执行连通性测试和数据包丢失统计操作控制，如图3-8所示。

网络诊断

连通性测试

使用Ping工具和TraceRoute工具，测试NF设备的连通性。

Ping工具

在文本框中输入IP地址，单击【Ping】按钮，即可探测到达指定目的地址的网络连接状态。

TraceRoute工具

在文本框中输入IP地址，单击【TraceRoute】按钮，即可追踪到指定目的地址的网络路由的情况。

数据包丢失统计信息

数据包丢失统计信息用于基于五元组参数和接口进行流量统计。根据用户配置的参数，查询转发报文数量和丢弃报文数，有助于技术支持人员进行故障排查。

控制

默认情况下，丢包统计操作未开启。

单击“丢包统计操作”右侧的【开始】按钮，NF根据配置区域设置的参数进行丢包统计，并在丢包统计结果实时显示统计数据。

在网络问题定位结束后，单击“丢包统计操作”右侧的【停止】按钮，停止统计数据，并在丢包统计结果显示最终统计数据。

配置

配置丢包统计的参数，以统计符合条件的数据流的报文数。数据包丢失统计参数说明如表3-9所示。

丢包统计操作

丢包统计结果

以列表的方式，展示各物理接口的报文统计数据，包括入方向报文数、出方向报文数和出方向丢弃报文数。

单击列表上方的【清空】按钮，可以清空统计的丢包信息。

进入 系统 > 维护工具 > 转发消息 页面，如图3-9所示。

转发消息

介绍如何检测系统中是否已存在指定的交换、路由信息、是否存在二层环路，以及如何查看所有路由信息。

路由信息

在如图3-9所示的页面中，路由区域默认展示静态路由的配置信息，同时支持切换展示策略路由的配置信息，支持设置查询条件进行查询。

交换自测

交换自测工具用于检测指定的二层交换MAC地址信息是否已经存在。

在如图3-9所示的页面中，在“转发检查”区域进入 交换自测 页签，配置参数，然后单击【自测】按钮，即可在下方显示自测结果。交换自测参数说明如表3-10所示。

交换自测参数

路由自测

路由自测用于检测指定的路由信息是否已经存在。

在如图3-9所示的页面中，在“转发检查”区域进入 路由自测 页签，配置源IP地址、目的IP地址。单击【自测】按钮，即可在下方看到路由自测结果。

二层环路检测

二层环路检测可以帮助用户检测二层链路上是否存在环路。

在如图3-9所示的页面中，在“转发检查”区域进入 二层环路检测 页签，配置二层环路探测的时间间隔，单击“状态”右侧的开关按钮，启用全局二层环路检测功能。单击【自测】按钮，即可开启所有接口的二层环路检测功能。

二层环路检测功能开启后，系统将在 系统日志 记录检测到的二层环路信息。

进入 系统 > 维护工具 > 抓包与回放 页面，如图3-10所示。

抓包与回放

用户可以在设备接口上直接抓取数据包，并打包下载。同时，可以直接导入抓包文件进行回放，用于分析、调试网络部署中遇到的问题。

抓包

在“配置”区域配置抓包任务的各项参数。抓包任务参数说明如表3-11所示。

抓包任务参数

单击【确定】按钮，保存配置。

单击【恢复默认设置】即可清空所有已配置参数。

单击【开始】按钮，开始抓包任务。

结束抓包任务。

根据抓包时间(秒)，结束抓包任务。

抓包过程中，单击【停止】按钮，结束抓包任务。

单击【下载】按钮，将抓包文件下载至本地，并进行分析。

----结束

回放测试

NF支持通过监听接口读取抓包文件，帮助用户分析网络数据。

回放测试的操作如下：

在如图3-10所示的页面中，在【回放接口】下拉列表中，选择回放测试中数据流入的监听接口。

在回放测试区域选择 抓包 下载的PCAP文件或者直接将PCAP文件拖动至指定区域。

----结束

报文示踪用于查看数据包在NF中的转发流程，方便绿盟科技的技术支持人员排查问题。开启报文示踪功能后，当满足条件的数据报文经过NF时，NF即可完成报文示踪功能。

进入 系统 > 维护工具 > 报文示踪 页面，如图3-11所示。

默认情况下，报文示踪功能关闭。

报文示踪

配置报文示踪参数，参数说明如表3-12所示。

支持设定一个或多个条件，当设定多个条件时，各个条件之间是“与”的关系。

报文示踪参数

单击【确定】按钮，保存设置。

若要将所有参数恢复默认配置，单击【恢复默认配置】按钮。

单击【开始】按钮，开启报文追踪功能。

单击【停止】按钮，结束报文追踪。

单击【下载】按钮，获取报文示踪的详细内容。

----结束

会话信息是NF设备记录TCP、UDP、ICMP等协议连接状态，实现报文正常转发的关键表项。在出现网络连接故障时，可以通过查看会话信息，以确定设备是否正常接收到报文。

进入 系统 > 维护工具 > 会话信息 页面，可以查看当前正在通过设备进行通信的会话的信息，同时支持批量删除和清空会话，如图3-12所示。

支持以下几种会话信息的查看方式：

直接单击【＜】、【＞】逐页查看。

设定查询条件查询会话信息，请参见 查询会话信息。

会话信息查看

配置会话管理

在图3-12中的“会话管理”区域，用户可以设置各类协议连接的参数信息，单击【保存】按钮。会话管理的参数说明如表3-13所示。

通常情况下，建议采用NF设备提供的默认老化时间，以保证各个协议的会话正常运行。

会话管理参数

查询会话信息

用户可以设定一个或多个条件，单击【查询】按钮，即可查询满足条件的会话信息。

当设定多个条件时，各个条件之间是“与”的关系。

删除会话信息

单击【清空】按钮，可以清空符合查询条件的所有会话。单击【批量删除】按钮，可以批量删除已勾选的会话信息。单击操作栏的【删除】，可以删除指定的会话信息。

查看会话详细信息

单击某条会话信息右侧操作栏的【更多】，即可查看该会话的详细信息。

进入 系统 > 维护工具 > 网络连接状态 页面，可以查看系统的网络连接状态和Server状态信息。

NET信息展示：可以查看系统的网络连接状态，包括使用的协议及端口等。

Server信息展示：单击不同的页签，可以查看缓存信息、接口信息和会话池信息。

进入 系统 > 维护工具 > 网卡状态 页面，可以查看到如下信息：

网卡状态：显示接口为UP且包含M口和H口的各个网卡状态信息，可以帮助用户排查网卡故障。

物理接口统计信息：显示各个业务口的收发报文统计信息。

进入 系统 > 维护工具 > 信息采集 页面，可以进行如下操作：

导出Kdump信息：单击【导出】按钮，即可导出Kdump信息。

导出一键巡检信息：选择导出的信息类型（Debug或Warning）后，单击【导出】按钮，即可将相应的信息导出到文件中。

仅在插入国密卡的NF设备中显示该菜单项。

进入 系统 > 维护工具 > 密码卡信息 页面，在成功检测到密码卡后，可以执行以下操作：

查看密码卡信息，包括密码卡型号、厂商、SN、商用密码产品认证证书编号。

单击【一键自检】按钮，检查密码卡的状态。

介绍NF的基本配置、系统控制、告警配置、远程协助和分片与重组的配置方法。

基本配置，是指配置NF引擎的基本参数。

进入 系统 > 系统配置 > 基本配置 页面，配置引擎参数。引擎参数说明如表3-14所示。

引擎参数

进入 系统 > 系统配置 > 系统控制 页面，如图3-13所示。

系统控制

端口控制

端口控制用于配置NF上需要关闭的端口列表。

在如图3-13所示的页面中，在TCP端口和UDP端口区域分别设置需要关闭的TCP和UDP协议的端口，单击【保存】按钮完成参数配置。

端口号的设置范围为0～65535，每行设置1个端口或者1个范围（例如：1或10:100）。

系统操作

在系统操作区域，单击相应的图标，可以对整个系统进行控制，详细说明如表3-15所示。

系统操作说明

进入 系统 > 系统配置 > 告警配置 页面，可以进行系统告警参数配置、Syslog服务器参数配置、邮件告警参数设置和触点告警参数设置，如图3-14所示。

告警配置

告警参数设置

在如图3-14所示页面的“告警参数设置”区域，配置触发邮件告警的事件的参数，参数说明如表3-16所示。

告警参数

邮件告警设置

NF支持邮件告警功能。该功能默认关闭，用于开启邮件发送功能，配置邮件告警的SMTP服务器的相关参数，以及触发邮件告警的事件参数。

在如图3-14所示的页面中，开启发送告警邮件功能后，配置发送告警邮件参数，参数说明如表3-17所示。

发送告警邮件参数

Syslog配置

在如图3-14所示页面的“Syslog配置”区域，可以选择日志模板（不同的模板将发送不同的日志格式），以及配置2个Syslog服务器的参数，包括IP地址、端口和通信秘钥，用于接收NF设备的日志。

NF可以向Syslog服务器发送的日志类型包括防火墙日志、入侵防护日志、防病毒日志、URL过滤日志、VPN日志和系统日志。关于NF向Syslog服务器发送的日志的类型及格式的说明，请参见 向syslog发送的日志格式简介。

触点告警设置

触点告警设置用于在License证书临近有效期或过期后，发送提醒信息，以便客户根据提示及时更新许可证，避免证书过期导致设备无法正常使用。

在正式许可证到期前3个月，首次给出提醒。

在试用许可证到期前7天，首次给出提醒。

在如图3-14所示页面的“触点告警设置”区域，配置告警频率和邮件告警参数。参数说明如表3-18所示。

触点告警参数

远程协助功能仅限绿盟科技的技术人员在网络调试时使用，建议用户忽略该功能。

一般情况下，用户会根据网络应用的实际情况设置接口的MTU（最大传输单元）值。NF设备中，MTU的取值范围为128～1600字节，大于MTU的IP数据报将被分为多个合适大小的数据报向目标主机传输，即为IP分片。目标主机接收到分片数据报后，必须通过分片处理将其还原为一个完整的IP数据报。

NF设备作为目标主机接收到分片数据报后，有2种分片处理方式：

分片重组

目标主机接收到分片后，利用分片时发送主机赋给每个数据报的ID（存放在标识符字段中）对数据报进行重组。

分片透传

目标主机接收到分片数据报后，不做任何处理。

进入 系统 > 系统配置 > 分片与重组 页面，配置分片处理参数，参数说明如表3-19所示。

分片处理参数

进入 系统 > 备份恢复 页面，即可备份及恢复全部配置文件，如图3-15所示。

备份恢复

配置备份

在“配置备份”区域选择“全部配置文件”，单击【下载备份】按钮，将全部配置文件下载至本地。

配置恢复

在“配置恢复”区域选择需要恢复的备份文件，确认后即可完成配置恢复。

NF中的SNMP支持SNMP V3版本，兼容SNMP V1和V2C版本。

进入 系统 > SNMP 页面，如图3-16所示。

SNMP

NF中SNMP的相关配置包括：

基础信息配置：SNMP Agent和Trap的总体开关、设备的信息配置、私有MIB库下载，以及SNMP相关说明文件下载。

SNMP配置：包括SNMP Agent和Trap的配置。

基础信息配置

在如图3-16所示的页面中，在“基础信息配置”区域配置SNMP Agent和Trap的总体开关、设备的信息配置、私有MIB库下载，以及SNMP相关说明文件下载。参数说明如表3-20所示。

SNMP系统信息配置

SNMP配置

在如图3-16所示的页面中，首先要选择SNMP的版本，可选项有：V1V2、V3。然后分别配置Agent和Trap的参数，不同的版本参数略有不同。

Agent配置

SNMP Agent用于在NF上收集可以向NMS报告的信息，支持SNMP V1V2和V3。

V1V2：采用团体名（Community Name）认证，非设备认可的团体名的SNMP报文将被丢弃。

V3：采用用户名和密码认证方式，而且支持加密传输，比V1和V2提供了更高的安全性。

单击Agent配置列表右上方的【新建】按钮，配置参数，即可新建一个SNMP Agent。参数说明如表3-21所示。不同版本的SNMP页面会有区别。

新建Agent后，可以对其进行编辑、查询和删除等操作。

SNMP Agent参数

Trap配置

NF可以主动向SNMP NMS发送SNMP Trap告警信息。NF的SNMP Trap支持SNMP V1、V2C和V3。此处配置接收告警事件Trap信息的SNMP NMS的IP地址和端口。

单击Trap配置列表右上方的【新建】按钮，配置参数，即可新建一个SNMP Trap。参数说明如表3-22所示。不同版本的SNMP页面会有区别。

新建Trap后，可以对其进行编辑、查询和删除等操作。

SNMP Trap主机参数

NF支持接入SD-WAN控制器（NSFOCUS Software-Defined WAN，简称SD-WAN），受SD-WAN统一集中管理。通过SD-WAN纳管NF设备，实现流量智能调度，提升用户体验，简化边界安全的部署与运维。

进入 系统 > SD-WAN 页面，配置SD-WAN参数，即可连接SD-WAN。参数说明如表3-23所示。

SD-WAN配置参数

虚拟系统功能模块用于管理虚拟系统资源和虚拟系统。

资源类用于定义各种资源数量。创建虚拟系统前需要先创建要引用的资源类。

进入 系统 > 系统 > 虚拟系统 > 资源类 页面，如图3-17所示。页面左侧以环形图和柱状图展示虚拟系统资源占用情况，页面右侧以列表展示系统已有的资源类。

虚拟系统资源占用统计图说明以及资源类说明分别如表3-24和表3-25所示。

资源类

虚拟系统资源占用统计图说明

资源类列表说明

新建资源类

进入 系统 > 系统 > 虚拟系统 > 资源类 页面，单击【新建】按钮，在侧滑框中配置资源类参数，单击【确定】按钮即可新建资源类。新建的资源类支持查询和删除。资源类参数说明如所表3-26示。

新建资源类参数

进入 系统 > 系统 > 虚拟系统 > 虚拟系统 页面，启用/禁用虚拟系统和查看虚拟系统列表，如图3-18所示。

虚拟系统

编辑虚拟系统

进入 系统 > 系统 > 虚拟系统 > 虚拟系统 页面，单击虚拟系统列表操作栏的【编辑】，可以修改虚拟系统参数。参数说明如表3-27所示。

删除虚拟系统

进入 系统 > 系统 > 虚拟系统 > 虚拟系统 页面，单击虚拟系统列表操作栏的【删除】，即可删除虚拟系统。不支持批量删除。

新建虚拟系统

进入 系统 > 系统 > 虚拟系统 > 虚拟系统 页面，单击【新建】按钮，配置虚拟系统参数，即可新建虚拟系统，虚拟系统参数说明如表3-27所示。

虚拟系统参数

接口绑定/解绑规则

虚拟系统接口绑定

为虚拟系统绑定接口的步骤如下：

使用weboper帐号登录NF。

进入 系统 > 系统 > 虚拟系统 > 虚拟系统 页面，如图3-19所示。

虚拟系统页面

在虚拟系统列表中，单击虚拟系统操作列的【编辑】。

在侧滑框“接口”区域左侧选中要绑定的接口，单击按钮，将接口添加到“接口”区域右侧框，如图3-20所示。

编辑虚拟系统

单击【确定】按钮，接口绑定成功。

----结束

登录虚拟系统 后，进入 系统 > 系统 > 帐号管理 > 虚拟系统帐号信息 页面，查看登录帐号的帐号信息和登录帐号对应的角色权限。

虚拟系统仅支持查看登录帐号自身的帐号信息和角色权限。虚拟系统帐号不支持新建和删除，仅支持编辑部分帐号参数，参数说明如表3-3所示。

登录虚拟系统

本节以Chrome浏览器为例，介绍weboper_vsysn首次登录NF虚拟系统的步骤。

打开浏览器，用HTTPS方式连接NF的管理IP地址，例如：https://192.168.1.1。

回车后显示安全报警信息，如图1-6所示。

登录前的安全报警

单击【高级】按钮，页面如图1-7所示。

查看高级选项

选择【继续前往…】，跳转到NF的Web登录页面，如图1-8所示。

Web登录

输入用户名和密码。

初次登录虚拟系统时，系统用户名和密码相同。例如初次登录虚拟系统vsys2时，帐号和密码均为weboper_vsys2。

单击【登录】按钮。

在弹出框中配置新密码，单击【确定】按钮。

使用新密码重新登录虚拟系统，默认进入 总览 页面，如图3-24所示。

虚拟系统登录首页

----结束

启用webpolicy_vsysn和webaudit_vsysn帐号

新建虚拟系统后，weboper_vsysn帐号默认开启，webpolicy_vsysn和webaudit_vsysn帐号默认关闭。

启用webpolicy_vsysn和webaudit_vsysn帐号的步骤如下：

使用weboper_vsysn帐号登录虚拟系统，参见 登录虚拟系统。

进入 系统 > 帐号管理 > 虚拟系统帐号信息 页面，如图3-25所示。

虚拟系统帐号信息

单击webpolicy_vsysn和webaudit_vsysn帐号启用栏的开关。

在弹出框中配置初始化密码，单击【确定】按钮，即可启用webpolicy_vsysn和webaudit_vsysn帐号。

配置webpolicy_vsysn和webaudit_vsysn帐号初始化密码

----结束