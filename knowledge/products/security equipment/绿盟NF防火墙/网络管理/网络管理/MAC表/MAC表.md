# MAC表

MAC表用于配置转发二层数据报文的规则。通过配置静态MAC表项，可以实现指定VLAN和MAC地址的设备只能通过指定端口接入网络，当设备移动到其他端口时，也将不能再通过动态学习获得MAC表项。而未配置MAC表项的设备，当设备通过某个接口接入时，自动通过动态学习来建立动态MAC表项。

因此，MAC表中的表项分为如下2类：

动态MAC表项：NF根据接收的二层报文动态学习到的MAC表项。

静态MAC表项：手动添加的MAC转发表项，用于配置转发二层数据包的规则。系统转发二层报文时，将根据报文的VLAN ID以及MAC地址，判断MAC表中是否有匹配的静态表项。如果有，通过静态表项中配置的接口对报文进行转发。

配置MAC表信息

进入 网络管理 > 网络管理 > MAC表 页面，单击【新建】按钮，配置静态MAC绑定参数。单击【确定】按钮，完成静态MAC表项配置。静态MAC参数说明如表4-14所示。

新建MAC表后，可以对其进行编辑、删除、启用/禁用、查找、清理和绑定操作。

静态MAC绑定参数

MAC绑定策略全局配置

MAC表老化时间，是指MAC动态表项从地址表中删除之前的生存时间。若超过老化时间，将该表项从地址表中删除。

单击页面顶部“收起”右侧的，可以查看到MAC表老化时间默认为4分钟，取值范围为0～255分钟。修改后单击【确定】按钮，完成修改。