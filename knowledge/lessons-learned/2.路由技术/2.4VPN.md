# 2.4 VPN

## 2.4.1 GRE VPN 故障排查

GRE VPN 定位故障的思路是：先查配置，再查看接口状态，然后查两端之间的网络，最后查看私网路由。

### 检查两端 GRE 配置

分别查看两端 GRE 配置是否正确。

**命令：** `display current-configuration interface Tunnel number`

例如：通过命令查看，可以确认两端 GRE 当前配置。

本端：
```bash
[H3C-A]display current-configuration interface Tunnel1
interface Tunnel1
 ip address 10.0.0.1 255.255.255.252
 source 192.168.1.1
 destination 192.168.2.1
```

对端：
```bash
[H3C-B]display current-configuration interface Tunnel1
interface Tunnel1
 ip address 10.0.0.2 255.255.255.252
 source 192.168.2.1
 destination 192.168.1.1
```

### 查看接口状态

分别查看两端 tunnel 接口协议是否 Up。

**命令：** `display interface Tunnel number`

例如：通过命令查看，可以确认 tunnel 接口的协议是 Up 的。

本端：
```bash
[H3C-A]display interface Tunnel1
Tunnel1 current state: Up
Line protocol current state: Up
Description: Tunnel1 Interface
The Maximum Transmit Unit is 1476
Internet Address is 10.0.0.1/30 Primary
Encapsulation is TUNNEL, service loopback-group ID not set
Tunnel Source 192.168.1.1 (GigabitEthernet0/1), destination 192.168.2.1
Tunnel bandwidth 64 (kbps)
Tunnel keepalive disabled
Tunnel protocol/transport GRE/IP
GRE key disabled
Checksumming of GRE packets disabled
```

对端：
```bash
[H3C-B]display interface Tunnel1
Tunnel1 current state: Up
Line protocol current state: Up
Description: Tunnel1 Interface
The Maximum Transmit Unit is 1476
Internet Address is 10.0.0.2/30 Primary
Encapsulation is TUNNEL, service loopback-group ID not set
Tunnel Source 192.168.2.1 (GigabitEthernet0/1), destination 192.168.1.1
Tunnel bandwidth 64 (kbps)
Tunnel keepalive disabled
Tunnel protocol/transport GRE/IP
GRE key disabled
Checksumming of GRE packets disabled
```

### 检查两端之间的网络

Tunnel 口状态为 Down，需要分为以下两种情况排查。

**Tunnel 接口下没有配置 Keepalive**，此时需要查看是否存在到达 Destination IP 的公网路由。

**命令：** `display ip routing-table`

例如：通过查看路由表，可以确认到 Destination IP 地址为 192.168.2.1 的路由存在。

```bash
[H3C-A]display ip routing-table
Routing Tables: Public
Destinations : 2       Routes : 2
Destination/Mask    Proto   Pre  Cost        NextHop         Interface
192.168.2.0/24      Direct  0    0           192.168.1.2     GE0/1
192.168.100.0/24    Static  60   0           10.0.0.1        Tunnel1
```

**Tunnel 接口下配置了 Keepalive**，此时不仅需要查看是否存在到达 Destination IP 的公网路由，还需要确认 Destination IP 是否可达。

**命令：** `ping -a source-ip destination-ip`

例如：通过命令可以看到，在 H3C-A 上指定源 192.168.1.1 ping H3C-B 的 192.168.2.1 可以 ping 通。

```bash
[H3C-A]ping -a 192.168.1.1 192.168.2.1
PING 192.168.2.1: 56 data bytes, press CTRL_C to break
Reply from 192.168.2.1: bytes=56 Sequence=1 ttl=255 time=11 ms
Reply from 192.168.2.1: bytes=56 Sequence=2 ttl=255 time=15 ms
Reply from 192.168.2.1: bytes=56 Sequence=3 ttl=255 time=15 ms
Reply from 192.168.2.1: bytes=56 Sequence=4 ttl=255 time=15 ms
Reply from 192.168.2.1: bytes=56 Sequence=5 ttl=255 time=15 ms

--- 192.168.2.1 ping statistics ---
5 packet(s) transmitted
5 packet(s) received
0.00% packet loss
round-trip min/avg/max = 11/15/15 ms
```

### 检查私网路由

查看到远端私网的路由是否指向了本端的 Tunnel 接口。

**命令：** `display ip routing-table`

例如：通过查看路由表，可以确认到远端私网 192.168.100.0/24 网段的路由指向了 Tunnel1 接口。

```bash
[H3C-A]display ip routing-table
Routing Tables: Public
Destinations : 2       Routes : 2
Destination/Mask    Proto   Pre  Cost        NextHop         Interface
192.168.2.0/24      Direct  0    0           192.168.1.2     GE0/1
192.168.100.0/24    Static  60   0           10.0.0.1        Tunnel1
```

## 2.4.2 L2TP VPN 故障排查

L2TP VPN 定位故障的思路是：先确认 LAC、LNS 的连通性，再查看 L2TP 隧道是否建立，最后查看用户的认证。

### 检查 LAC 与 LNS 是否路由可达

分别查看 LAC、LNS 路由器的路由表中是否存在到达对端的路由，并确认是否路由可达。

**命令：** `display ip routing-table`

例如：通过命令查看，可以确认 LAC 路由器上存在到达对端 LNS 的路由 0.0.0.0/0，下一跳是 10.0.0.1。

```bash
[LAC]display ip routing-table 0.0.0.0
Routing Tables: Public
Destinations : 1       Routes : 1
Destination/Mask    Proto   Pre  Cost        NextHop         Interface
0.0.0.0/0           Static  60   0           10.0.0.1       LTE1/0/0
```

检查 LAC 和 LNS 之间是否路由可达。

**命令：** `ping ip-address`

例如：通过命令可以看到，LAC 到达 LNS 之间路由可达。

```bash
[LAC]ping 10.0.1.2
PING 10.0.1.2: 56 data bytes, press CTRL_C to break
Reply from 10.0.1.2: bytes=56 Sequence=1 ttl=255 time=32 ms
Reply from 10.0.1.2: bytes=56 Sequence=2 ttl=255 time=25 ms
Reply from 10.0.1.2: bytes=56 Sequence=3 ttl=255 time=27 ms
Reply from 10.0.1.2: bytes=56 Sequence=4 ttl=255 time=23 ms
Reply from 10.0.1.2: bytes=56 Sequence=5 ttl=255 time=23 ms

--- 10.0.1.2 ping statistics ---
5 packet(s) transmitted
5 packet(s) received
0.00% packet loss
round-trip min/avg/max = 23/26/32 ms
```

### 查看 L2TP 隧道是否建立

查看 LAC、LNS 之间的 L2TP 隧道是否正常建立。

**命令：** `display l2tp tunnel`

例如：通过命令查看，可以确认 L2TP 隧道已成功建立。

```bash
[LAC]display l2tp tunnel
Total tunnel: 1
TunnelID  LocalTID RemoteTID RemoteAddress   Port Sessions RemoteName
1         1741     1         10.1.1.2         1701 1        Ins
```

### 检查 LAC、LNS 上是否使能 L2TP

确认 LAC、LNS 路由器配置中是否存在 `l2tp enable` 命令。

**命令：** `display current-configuration include l2tp enable`

例如：通过命令查看，可以确认 LAC 路由器上存在 `l2tp enable` 命令，使能了 L2TP 功能。

```bash
[LAC]display current-configuration include l2tp enable
l2tp enable
```

```bash
[LNS]display current-configuration include l2tp enable
l2tp enable
```

### 检查 LAC、LNS 两侧 L2TP 配置

确认 LAC、LNS 路由器 L2TP 配置中隧道验证、隧道名称参数是否对应。

确认 LAC 路由器配置的发起 L2TP 连接请求参数是否正确。

确认 LNS 路由器配置的接收 L2TP 连接请求参数是否正确。

```bash
[LAC]display current-configuration
l2tp enable
domain system
 authentication ppp
 local-access-limit disable
 state active
 idle-cut disable
 self-service-url disable
local-user User1
 password cipher $c$3$WIYxAfpVMZsCDelUxUGBIEPR2OOGRG+G
 service-type ppp
l2tp group 1
 undo tunnel authentication
 start l2tp ip 10.0.1.2 domain system
interface Virtual-Template1
 ppp authentication-mode chap domain system
 peer ip address 11.1.1.1 255.255.255.255
interface Ethernet0
 port link-mode route
 pppoe-server bind Virtual-Template1
```

```bash
[LNS]display current-configuration
l2tp enable
domain system
 authentication ppp
 local-access-limit disable
 state active
 idle-cut disable
 self-service-url disable
 pool 1 192.168.0.2 192.168.0.10
local-user User1
 password cipher $c$3$nH3DITBXIRRbVjEB+IUxmsj+2LFtow
 service-type PPP
l2tp group 1
 undo tunnel authentication
 allow l2tp virtual-template 1
interface Virtual-Template1
 ppp authentication-mode chap domain system
 remote address pool 1
 ip address 192.168.0.1 255.255.255.0
```

### 查看 L2TP 会话

查看 LNS 上是否存在用户 L2TP 会话。

**命令：** `display l2tp session`

例如：通过命令查看，可以确认本端已存在一个 L2TP 会话。

```bash
[LNS]display l2tp session
Total session: 1
SessionID  LocalSID RemoteSID LocalTID
17922       17922    12990     1
```

### 检查用户认证相关设备的配置

确认 LNS 路由器配置的用户认证方式是本地认证，还是远端 AAA 认证。

如果是本地认证，则确认输入的用户名密码是否与 LNS 路由器上配置的一致。

如果是远端 AAA 认证，则确认 AAA 配置是否正确，AAA 服务器上配置是否正确。

**命令：** `debugging ppp all terminal debugging terminal monitor`

例如：通过命令可以查看到由于 AAA 服务器上配置的密码与用户拨入密码不符，验证失败的信息。

```bash
[LNS>debugging ppp all
[LNS>terminal debugging
[LNS>terminal monitor
//PPP 向 AAA 服务器发送认证信息

Jul 14 09:14:212 2013 H3C PPP/7/DEBUG:
PPP Event: Virtual-Template1:0 LCP send CHAP Packet: ulPacketID = 00000002, ulUserID = Detail
AAA Tunnel[0] 65535, ulVlanTag[1] 65535
ulServiceType = 10005, ulUserType = 01
SCline_number = 0

Jul 14 09:14:215 2013 H3C PPP/7/DEBUG:
PPP 侧从 AAA 侧收到认证失败的信息
Packet Level: Virtual-Template1:0 Receive AAA Reject Acknowledge
Authen Result = FAIL

Jul 14 09:14:215 2013 H3C PPP/7/DEBUG:
PPP Event: Virtual-Template1:0 CHAP Auth Result Event State WaitingAAA ServerFailed

//PPP 报错，chap 验证失败

Jul 14 09:14:215 2013 H3C PPP/7/DEBUG:
PPP Error: Virtual-Template1:0 CHAP Server authentication failed. No

Jul 14 09:14:216 2013 H3C PPP/7/DEBUG:
PPP State Change: Virtual-Template1:0 CHAP WaitingAAA -> ServerFailed
//PPP 侧告警，验证失败，原因为用户的密码非法

Jul 14 09:14:216 2013 H3C PPP/7/DEBUG:
PPP Packet: Virtual-Template1:0 Output CHAP(0xC223) Pkt, Len 33
State ServerFailed code FAILURE(41), id = 1, len = 29
Message: "Illegal user or password"
```

## 2.4.3 DVPN 故障排查

DVPN 采用 Client/Server 模式，工作在 TCP/IP 协议栈的应用层。

DVPN 支持 UDP 和 GRE 两种隧道类型，按照工作方式的不同，可将一个 VPN 域中的设备划分为一个 Server 和多个 Client，Server 的公网地址为静态地址，Client 的公网地址既可以静态配置也可以动态获取，而 Client 的私网地址则需要按照规划静态分配。在同一个 VPN 域内，要求所有节点的私网地址在同一个网段内。

DVPN 故障的定位思路是：首先检查设备间的连通情况，然后检查 VAM Server 上的设备注册情况，最后检查 Hub、Spoke 间的设备间的隧道建立、配置情况。

### 检查设备连通情况

分别查看不同设备间的网络连通情况，如果设备之间不能够 ping 通，则怀疑设备间物理连线或者接口配置有问题。

**命令：** `ping destination-ip-address`

例如：通过命令检查设备间的连接情况。

```bash
[H3C]ping 192.168.1.4
PING 192.168.1.4: 56 data bytes, press CTRL_C to break
Reply from 192.168.1.4: bytes=56 Sequence=1 ttl=255 time=1 ms
Reply from 192.168.1.4: bytes=56 Sequence=2 ttl=255 time=1 ms
Reply from 192.168.1.4: bytes=56 Sequence=3 ttl=255 time=1 ms
Reply from 192.168.1.4: bytes=56 Sequence=4 ttl=255 time=1 ms
Reply from 192.168.1.4: bytes=56 Sequence=5 ttl=255 time=1 ms

--- 192.168.1.4 ping statistics ---
5 packet(s) transmitted
5 packet(s) received
0.00% packet loss
round-trip min/avg/max = 1/1/1 ms
```

### 检查 VAM Server 上设备注册状态

若 Hub 和 Spoke 设备中某一设备没有注册到 VAM Server，则说明该设备与 VAM Server 之间的链路存在问题，下一步则要检查该设备与 VAM Server 上的配置参数是否一致。

**命令：** `display vam server address-map all`

例如：通过命令查看 VAM Server 上设备的注册状态，如果其中有一台 Spoke 或者 Hub 设备没有注册到 VAM Server 上，该设备将不会显示出来。所有的 Hub 和 Spoke 设备均已经注册到了 VAM Server。

```bash
<vamserver>display vam server address-map all
VPN name: vpn1
Total address-map number: 4

Private IP    Public IP     Type        Holding time
10.0.1.1      10.0.1.2      Hub         0d 0h 1m 19s
10.0.1.2      10.0.1.2      Hub         0d 1h 3m 57s
10.0.1.3      10.0.1.3      Spoke       0d 1h 4m 65s
10.0.1.4      10.0.1.4      Spoke       0d 1h 5m 25s

VPN name: vpn2
Total address-map number: 3

Private IP    Public IP     Type        Holding time
10.0.2.1      10.0.2.1      Hub         0d 1h 1m 19s
10.0.2.2      10.0.2.2      Hub         0d 1h 3m 57s
10.0.2.3      10.0.2.3      Spoke       0d 1h 4m 25s
```

### 检查 VAM Server 设备配置情况

在 VAM Server 需要检查监听 IP 地址、预共享密钥、本地用户名及密码，与 Hub 和 Spoke 设备上的设置是否一致，如果不一致将会导致 Hub 和 Spoke 设备无法注册到 VAM Server 上。

```bash
vam server ip 192.168.1.22
//指定 VAM Server 的监听 IP 地址
vam server vpn 1 id 1
//创建 VPN 域，ID 为 1
 pre-shared-key simple 123
 //设置预共享密钥
 hub private-ip 10.0.1.1
 hub private-ip 10.0.1.2
 //指定 VPN 域 1 的两个 Hub 地址
local-user dvpn1hub1 password simple dvpn1hub1
 authorization attribute level 3
 service-type dvpn
 //创建本地 Hub 用户
 //设置用户密码
 //服务类型设置为 DVPN
local-user dvpn1spoke1 password simple dvpn1spoke1
 authorization attribute level 3
 service-type dvpn
 //创建本地 Spoke 用户
 //设置用户密码
 //服务类型设置为 DVPN
```

### 检查 Hub 设备相关配置

检查 VAM Client 是否启动，Hub 设备上的相关配置参数要与服务器上的相关参数是否一致，必须保持一致，才能够使得该设备正确注册到 VAM Server 上。

```bash
vam client
 name dvpn1hub1
 client enable
 server primary ip-address 192.168.1.22
 //对应于 VAM Server 地址
 user dvpn1hub1 password simple dvpn1hub1
 //对应于 VAM Server 上用户名和密码
 pre-shared-key cipher simple 123
 //设置预共享密钥
```

### 检查 Spoke 设备相关配置

检查 VAM Client 是否启动，Spoke 设备上的相关配置参数要与服务器上的相关参数必须保持一致，才能够使得该设备正确注册到 VAM Server 上。

```bash
vam client
 name dvpn1spoke1
 client enable
 server primary ip-address 192.168.1.22
 //对应于 VAM Server 地址
 user dvpn1spoke1 password simple dvpn1spoke1
 //对应于 VAM Server 上用户名密码
 pre-shared-key cipher simple 123
 //设置预共享密钥
```

### 查看设备隧道建立情况

Hub 设备和 Spoke 设备之间，Hub 设备和 Hub 设备之间常见的问题主要是链路问题，可在 Hub 设备上检查 Hub 与其他设备之间建立起的隧道信息。

**命令：** `display dvpn session all`

例如：查看 Hub 设备上的隧道建立信息，如果有其中一台设备没有能与之建立起隧道信息，则将不会在信息中显示出来。其他 Hub 和 Spoke 设备与该设备之间均正常建立起了隧道。

```bash
[hub1]display dvpn session all
Interface: Tunnel1
VPN name: vpn1
Total number: 3

Private IP: 10.0.1.2  Public IP: 192.168.1.2
Session Type: Hub-Hub
State: SUCCESS  Holding time: 0h 10m 15s
Input: 83 packets, 82 data packets, 1 control packets, 0 multicasts, 0 errors
Output: 83 packets, 82 data packets, 1 control packets, 0 multicasts, 0 errors

Private IP: 10.0.1.3  Public IP: 192.168.1.3
Session Type: Hub-Spoke
State: SUCCESS  Holding time: 0h 12m 45s
Input: 98 packets, 97 data packets, 1 control packets, 0 multicasts, 0 errors
Output: 106 packets, 105 data packets, 1 control packets, 0 multicasts, 0 errors
```

从 Spoke 设备检查隧道的建立情况时，要首先考虑 DVPN 的组网类型是属于哪一种。

DVPN 有两种典型的组网结构，Full-Mesh 网络和 Hub-Spoke 网络。

- Full-Mesh 网络中，Spoke 之间可以建立隧道直接通信，Hub 主要作为路由信息交换的中心
- Hub-Spoke 网络中，Spoke 之间不能建立隧道直接通信，只能通过 Hub 转发数据。Hub 既作为路由信息交换的中心，又作为数据转发的中心。

**命令：** `display dvpn session all`

例如：在 Hub-Spoke 网络中，通过该命令检查，Spoke 与 Hub 之间建立起的隧道情况，当链路不通时，将无法建立。

```bash
[spoke1]display dvpn session all
Interface: Tunnel1
VPN name: vpn1
Total number: 2

Private IP: 10.0.1.2  Public IP: 192.168.1.2
Session Type: Spoke-Hub
State: SUCCESS  Holding time: 0h 10m 25s
Input: 258 packets, 257 data packets, 1 control packets, 0 multicasts, 0 errors
Output: 727 packets, 726 data packets, 1 control packets, 0 multicasts, 0 errors

Private IP: 10.0.1.1  Public IP: 192.168.1.1
Session Type: Spoke-Hub
State: SUCCESS  Holding time: 0h 43m 35s
Input: 299 packets, 298 data packets, 1 control packets, 0 multicasts, 0 errors
Output: 291 packets, 290 data packets, 1 control packets, 0 multicasts, 0 errors
```

### 检查 Hub 隧道配置

在 Hub 上需要检查隧道配置参数与其他 Hub 和 Spoke 上的隧道配置参数是否一致，对等体预共享密钥、安全协议验证算法、Tunnel 封装协议以及网络类型是否一致，如果不一致将会导致隧道无法建立起来。

```bash
ike peer vam
 pre-shared-key abcde
 //配置 IKE 对等体
ipsec transform-set vam
 encapsulation-mode tunnel
 transform esp
 esp authentication-algorithm sha1
 //配置 IPSec 安全提议
ipsec profile vam
 pfs dh-group2
 ike-peer vam
 transform-set vam
 sa duration traffic-based 6000
 //配置 IPSec 安全框架
interface Tunnel1
 ip address 10.0.1.1 255.255.255.0
 tunnel-protocol dvpn udp
 source GigabitEthernet0/0
 ospf network-type broadcast
 ipsec profile vam
 //隧道封装协议类型
 //网络类型
 vam client name dvpn1hub1
```

### 检查 Spoke 隧道配置

在 Spoke 设备上检查隧道配置参数，需要考虑网络类型，如果是 Full-Mesh 类型的网络，需要检查与其他 Hub 和 Spoke 上的隧道配置参数是否一致；如果是 Spoke-Hub 网络类型，则只需要检查与对端 Hub 上隧道参数是否一致。然后检查对等体预共享密钥、安全协议验证算法、Tunnel 封装协议以及网络类型是否一致，如果不一致将会导致隧道无法建立起来。

```bash
ike peer vam
 pre-shared-key abcde
 //配置 IKE 对等体
ipsec transform-set vam
 encapsulation-mode tunnel
 transform esp
 esp authentication-algorithm sha1
 //配置 IPSec 安全提议
ipsec profile vam
 pfs dh-group2
 ike-peer vam
 transform-set vam
 sa duration traffic-based 6000
 //配置 IPSec 安全框架
interface Tunnel1
 ip address 10.0.1.3 255.255.255.0
 tunnel-protocol dvpn udp
 source GigabitEthernet0/0
 ospf network-type broadcast
 ipsec profile vam
 //隧道封装协议类型
 //网络类型
 vam client name dvpn1spoke1
```
