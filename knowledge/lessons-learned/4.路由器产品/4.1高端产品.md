# 4.1 高端产品

## 4.1.1 MSR G2 设备 License 异常故障排查

MSR G2 系列路由器是搭载了 H3C 自主研发的 Comware V7 平台的新一代开放多业务路由器产品，其启动文件可以分为基本软件包和特性软件包。若用户只有一些基本的业务需求，则设备仅需要加载基本软件包即可；但若客户需要使用一些高级特性，例如 MPLS、语音等，则需要购买对应的软件授权函，成功激活后才可以在设备上加载特性软件包，满足客户的特性需求。

MSR G2 路由器的 License 和设备一一对应，设备 License 异常的定位故障的思路是：首先确认 License 能否正常激活，查看激活文件是否正确，再看当前 License 的使用状态是否正常，最后判断特性软件包是否已经正常加载。

### License 能否正常激活

客户使用软件授权函在官网完成 License 的激活之后会获得一个扩展名为 .ak 的激活文件。建议使用 TFTP 将激活文件上传到设备上，然后执行命令进行 License 激活操作。

正常情况下，License 可以被激活。但是如果激活文件有问题，就无法完成激活操作。

**命令：** `license activation-file install file-name`

例如：使用激活文件激活 License 时，正常情况下会激活并成功安装特性软件包；若激活文件有问题，则会提示激活失败。

```bash
[H3C]license activation-file install 210231AIUYB133000032201305330000.ak
[H3C]
%May 31 15:07:54:394 2014 H3C PKG/5/PKG: Installing package cfa0:/msr56-Cmw710-data-ROI06.bin on slot 0
%May 31 15:07:57:098 2014 H3C PKG/5/PKG: cfa0:/msr56-Cmw710-data-ROI06.bin installed on slot 0.
[H3C]license activation-file install cfa0:/210235A0W8B1380003280404190.ak
Failed to install the activation file.
```

### 确认激活文件是否正确

在使用扩展名为 .ak 的激活文件执行激活操作时，若无法正常激活 License 就一定是激活文件有问题。

激活文件异常只有两种可能：激活文件注册有误或激活文件被损坏。

在客户购买设备较多时，每台设备分别注册激活文件，可能会将一台设备的 SN 和另一台设备的 DID 混在一起注册激活文件，就导致了文件注册时出错。由于 MSR G2 设备的激活文件和设备一一对应，必须是同一台设备的 SN 和 DID 注册生成的激活文件，才可以用于激活该设备的特性软件包。可以通过命令查看激活文件中的设备信息是否和注册的设备一致，若信息有误，需进行 License 的解绑定操作，解绑之后再重新注册。

激活文件损坏，可能是传输异常或人为操作导致的。

Comware V7 平台是基于 Linux 内核的系统，由于 Windows 和 Linux 系统 ASCII 编码方式对字符的定义有差别，在使用 FTP 的 ASCII 模式向 MSR G2 路由器传输文件时，会导致文件的内容损坏。建议使用 TFTP 进行文件传输，也可以使用 FTP 的二进制模式传输文件。

License 激活文件必须是 H3C 官网下发的扩展名为 .ak 的原文件，若用户对激活文件的内容进行过编辑，也会导致激活文件不可用。

**命令：** `more file-url`
**命令：** `display license device-id`
**命令：** `license activation-file uninstall file-name`

例如：可以通过 `more` 命令查看激活文件的内容，若其中的设备信息与 `display license device-id` 显示的不完全一致，则需要进行激活文件的解绑定操作。

```bash
<H3C>more 210235A0T3B1330000972013042408525130572.ak
License data:
Type: 00010002
SN: 210235A01188138000507
Device ID: 7x@t#8BEV-CkK-Sp/07%XW-MYCL-djff-ThSHI

<H3C>display license device-id
SN: 210235A01188138000507
Device ID: 7x@t#8ZxSL-AG%x-TchC-YGTp-gsxH-wRcK-3mRd-a3S&
```

### 重新上传或申请激活文件

若是由于客户上传了编辑过的激活文件或是使用 FTP 的 ASCII 模式传输的激活文件，导致激活文件错误而无法激活，此时只需要保证使用 H3C 官网下发的原文件，并使用 TFTP 再次上传文件，即可正常的完成激活操作。

若是激活文件注册有误，则必须进行 License 的解绑定操作，解绑之后在官网进行重新注册。

License 的解绑定需由 H3C 的工程师提交工作联络单来完成，客户需要说明解除绑定的原因，提供激活成功的文件、授权序列号、最终用户单位名称、条码等信息。

**命令：** `tftp tftp-server get source-filename`
**命令：** `license activation-file install filename`

例如：使用 TFTP 上传扩展名为 .ak 的激活文件，完成 License 的激活操作，并成功加载特性软件包。

```bash
[H3C]tftp 1.1.1.2 get 210231A0W8B1330000322013053114203000.ak
Total 2296 bytes transferred in 0 second(s), 25421 bytes/second
[H3C]license activation-file install 210231AIUYB1330000322013053114203000.ak
[H3C]
%May 31 15:07:54:394 2014 H3C PKG/5/PKG: Installing package cfa0:/msr56-Cmw710-data-ROI06.bin
On slot 0
%May 31 15:07:57:098 2014 H3C PKG/5/PKG: cfa0:/msr56-Cmw710-data-ROI06.bin installed on slot 0
```

### 查看当前 License 是否 In use 状态

若设备的 License 能够正常激活，那么 License 的使用状态应该是 In use，只有这种状态下特性软件包才会被加载。

**命令：** `display license`

例如：查看当前设备上的 License 状态，In use 是正常状态。

```bash
<H3C>display license
cfa0:/license/210235A0NAA1290000012012102412349234836.a
Feature: pkg_license
Product Description: H3C MSR36 Data Software License
Registered at: 2013-05-09 08:41:12
License Type: Permanent
Current State: In use
```

### License 状态显示 Uninstalled 或 Expired

激活文件在被客户手动卸载之后，License 的当前状态就会显示为 Uninstalled。

MSR G2 路由器的 License 不能随意卸载，因为卸载之后该激活文件便失效。卸载后的激活文件在该设备上再安装时，会提示该文件已被卸载 "The activation file has been uninstalled"。

必须进行解绑定操作后，才可以在官网重新进行注册。

卸载设备上的激活文件后，会生成一个 uninstall 文件用于解绑定。License 的解绑定需由 H3C 的工程师提交工作联络单来完成，客户需要说明解除绑定的原因，提供激活成功的文件、授权序列号、最终用户单位名称、条码等信息。

在一些特殊的场景下，客户可能会使用试用版的 License。试用版的 License 都有一定的使用期限，在试用版的激活文件到期后，License 的当前状态就会显示为超期 Expired。显示超期的激活文件将不会生效，无法正常地加载对应的特性软件包。超期后的试用版激活文件将作废，无法解绑定也不可能再给任何设备使用。

若一台设备上有多个针对某一软件特性的激活文件，只要其中有一个处于 In use 的状态，该特性软件包就会被加载，其余已卸载或超期的激活文件对软件包的正常加载没有任何影响。

**命令：** `display license`

例如：查看当前设备上的 License 状态，已卸载和超期的 License 均无效。只有 License 类型为 Trial（试用版）的才可能超期。

```bash
<H3C>display license
cfa0:/license/210235A0W8B13B0004042014012213204451968.ak
Feature: pkg_license
Product Description: H3C MSR36 Data Software License
Registered at: 2014-01-22 13:45:00
License Type: Permanent
Current State: Uninstalled
Uninstall Date: 2014-01-22 13:45:46

cfa0:/license/210235A0WAA1290000012012102416054637474.ak
Feature: pkg_license
Product Description: H3C MSR36 Voice Software License
Registered at: 2013-10-22 08:41:12
License Type: Trial (Date restricted)
Trial Validity Period: 2013-10-22 to 2013-12-21
Current State: Expired
```

### 查看特性软件包是否加载

在设备被格式化后，通过命令 `display license` 查看到的 License 状态同样会是 In use。但是在 `display license` 下的 "Feature" 和 "Product Description" 项中不会有描述信息，说明 License 表项对应的文件已丢失或者损坏。

这是因为设备中存储的扩展名为 .ak 的激活文件实际已经不存在了，当前状态中显示的"使用中"信息其实也只是假象。由于激活文件的缺失，License 实际是未激活的状态，特性软件包自然也不可能被加载。此时需要工程师将之前保存的对应该设备的激活文件重新上传到设备上，并将文件 copy 到设备的 License 文件夹内，文件名必须和 `display license` 中显示的名字完全一致，之后设备可以自动识别文件，完成激活文件的加载。

但是由于此过程并非执行了安装激活文件的操作，因此特性软件包无法一起加载，需要由工程师手动来加载软件包，其操作过程详见后续步骤。

另外，在很偶然的情况下，也可能会出现扩展名为 .ak 的激活文件已经被成功注册到设备上，并且使用状态显示为 In use，但是相应的特性软件包却并没有被正常加载。这时也需要工程师来手动完成加载相应特性软件包的工作。

**命令：** `display license`
**命令：** `display install active`

例如：通过命令查看设备当前 License 的使用状态为 In use，但只加载了基本软件包，未加载对应的数据版特性软件包。

```bash
<H3C>display license
cfa0:/license/210235A0WAA1290000012012102412349234836.a
Feature: pkg
Product Description: H3C MSR36 Data Software License
Registered at: 2013-05-09 08:41:12
License Type: Permanent
Current State: In use

<H3C>display install active
Active packages on the device:
cfa0:/msr36-CMW710-boot-ROI06.bin
cfa0:/msr36-CMW710-system-ROI06.bin
```

### 手动加载软件包

在 License 状态显示为 In use，但是对应的特性软件包并未加载的情况下，需要工程师手动完成软件包的加载工作。

首先通过 install 命令手动加载特性软件包，然后 return、quit 退出命令行，之后再次通过命令行登录设备时，特性软件包就会被加载，其中支持的特性已经可以正常使用。最后切记要执行 `install commit` 命令，确认此次的安装操作。只有这样，在设备重启之后，仍会继续加载此次手动安装的特性软件包。

**命令：** `install activate feature file-name`
**命令：** `display install active`
**命令：** `install commit`

例如：手动加载特性软件包，并查看该软件包已被正常安装，之后确认此次安装操作。

```bash
[H3C]install activate feature cfa0:/msr36-cmw710-data-ROI06.bin
Upgrade summary according to following table:
cfa0:/msr36-cmw710-data-ROI06.bin
Running Version: NoneRelease
New Version: 0106
Upgrade Way: Service Upgrade
Upgrading software images, compatible versions
Continue? [Y/N]:y
This operation will take several minutes, please wait... Done.

<H3C>display install active
Active packages on the device:
cfa0:/msr36-cmw710-boot-ROI06.bin
cfa0:/msr36-cmw710-system-ROI06.bin
cfa0:/msr36-cmw710-data-ROI06.bin

<H3C]install commit
```

---

## 4.1.2 MSR 风扇故障排查

风扇是设备的重要组成部分，对于设备的正常工作起着不可或缺的作用。风扇转速由主控板控制，转速的调整范围在 50%~100% 之间。

风扇控制器设置了低温门限和高温门限两个参数，高温门限对应 100% 的风扇转速，低温门限对应 50% 的风扇转速。风扇停转时可以发出停转告警信号，任意一路风扇告警均能响应。

MSR 出现风扇故障，如果所处环境散热不好，可能会出现设备由于温度过高导致运行异常的情况。

MSR 风扇故障排错的定位思路是：查看风扇状态，查看日志中风扇告警情况，检查设备版本，检查风扇有无异响，检查出风口有无异常。

### 检查风扇状态

首先检查风扇的当前状态。

**命令：** `display fan`

例如：通过命令检查设备风扇的状态。

MSR V5 设备：
```bash
[H3C]display fan
Fan State: Normal
```

MSR V7 设备：
```bash
[H3C]display fan
Index Status Speedlevel
0     Normal 50
```

在设备风扇正常的情况下，风扇的 State 或者 Status 应为 Normal，当出现异常的时候，可能会出现 abnormal 或者 absent 状态。

然后可通过直观观察风扇指示灯的闪亮情况，来判定风扇状态。正常情况风扇指示灯应该为常绿，不同风扇运行情况，指示灯的状态如表 4-1 所示。

**表 4-1 指示灯状态**

| 指示灯 | 状态 | 含义 |
|--------|------|------|
| FAN | 灭 | 风扇不在位 |
| FAN | 绿色常亮 | 正常工作 |
| ALM | 黄色 | 风扇故障 |

### 风扇物理状态检查

直观地观察风扇是否在位，通过插拔风扇，测试风扇是否接触正常。

### 检查风扇告警情况

有些风扇问题，可能会出现时好时坏的情况，这时候在 log 信息中会有记录，检查风扇的告警信息。

**命令：** `display logbuffer`

例如：MSR V5 设备，通过该命令检查设备风扇的告警信息，当风扇不在位时，告警如下。

```bash
[H3C]display logbuffer
Logging buffer configuration and Contents:
enabled
Allowed max buffer size: 1024
Actual buffer size: 512
Channel number: 1
Channel name: logbuffer
Dropped messages: 0
Overwritten messages: 0
Current messages: 15
%Nov 24 15:53:26:950 2014 H3C SHELL/4/LOGIN:
Task: Cmd(CoO-IPAddr) User: H3C
Command: display fan
%Nov 24 15:53:34:220 2014 H3C DEVM/3/FAN_ABSENT:
Fan 1 is absent
```

另外在 MSR V5 设备上还可以查看风扇 trap 告警信息。

**命令：** `display trapbuffer`

例如：当风扇异常时，通过该命令检查风扇 trap 告警，可以看到风扇状态变为失败的 trap 信息。

```bash
[H3C]display trapbuffer
Trapping buffer configuration and Contents:
enabled
Allowed max buffer size: 1024
Actual buffer size: 256
Channel number: 1
channel name: trapbuffer
Dropped messages: 0
Overwritten messages: 0
Current messages: 2
#Nov 24 15:52:34:343 2014 H3C SHELL/4/LOGIN:
Trap 1.3.6.1.4.1.25506.2.2.1.1.3.0.1 <hh3cLogin>
login from H3C
#Nov 24 15:53:34:220 2014 H3C DEVM/4/FAN_STATE_CHANGES_TO_FAILURE:
Trap 1.3.6.1.4.1.25506.8.35.12.1.6 <hh3cFanFailure>
fan ID 1
```

当设备为 V7 设备时，可以通过如下命令查看风扇告警信息。

**命令：** `display logbuffer`

例如：当风扇异常时，利用该命令查看日志中风扇信息，会有 "Fan 1 failed" 告警信息。

```bash
[HI3C]display logbuffer
Log buffer:
Enabled
Max buffer size: 1024
Actual buffer size: 512
Dropped messages: 0
Overwritten messages: 0
Current messages: 11
%Oct 23 18:52:47:372 2013 MSR3600-H3C-A IFNET/3/PHY_UPDOWN:
Aux0 link status is up
%Oct 23 18:54:07:113 2013 MSR3610-H3C-A DEV/2/FAM_FAILED: Fan 1 failed
```

### 检查设备版本

当设备较陈旧时，可能会出现误检的情况，此时建议升级一下设备版本再进行观察。

查看设备版本。

**命令：** `display version`

例如：MSR56 双主控，在 ROI06P11 之前版本，当热插拔主用主控的时候，会导致风扇信息同步失败而出现误检的情况，查看版本信息如下。

```bash
[H3C]display version
H3C Comware Software
Version 7.1.049, Release 0104
Copyright (c) 2004-2013 Hangzhou H3C Tech. Co., Ltd.
All rights reserved.
H3C MSR56-60 uptime is 16 weeks 21 hours, 29 minutes
Last reboot reason: Power on
```

### 检查风扇有无异响

可以直观地通过观察风扇扇叶有无转动，是否处于无障碍运行状态，然后可以用于放置到风扇处，用手感知有没有风，正常情况下，可以看到风扇转动，用手可以感知到风。

### 检查设备出风口有无异物

可通过直接观察风扇出风口有无障碍物异物阻挡，如果有则建议清理。

---

## 4.1.3 PPPoE Client 故障排查

PPPoE（Point-to-Point Protocol over Ethernet，在以太网上承载 PPP 协议）的提出，解决了 PPP 无法应用于以太网的问题，是对 PPP 的扩展。PPPoE 将 PPP 报文封装在以太网帧之内，在以太网上提供点对点的连接。

PPPoE 可以通过一个远端接入设备为以太网上的主机提供互联网接入服务，并对接入的每个主机实现控制、计费功能。

PPPoE 定位故障的思路是：先查看配置是否正确，之后根据协商阶段查看 Discovery 发现阶段与 Session 会话阶段报文交互过程。

### 检查 PPPoE 配置

检查配置应关注，感兴趣流是否匹配可以触发拨号，以太网口与 Dialer 拨号口是否进行关联，PPP 验证与 IP 地址分配是否正确。

**命令：** `display current-configuration`

例如：在设备上通过 `display current-configuration` 查看配置是否正确。

```bash
[H3C]display current-configuration
interface Dialer1
# 创建拨号口
link-protocol ppp
ppp pap local-user 123 password simple 123
# 配置 PPP 认证用户名与密码
ip address ppp-negotiate
# 配置 IP 地址
dialer user 123
# 配置拨号用户名
dialer-group 1
# 配置拨号组与触发条件绑定
dialer bundle 1
# 配置 Dialer bundle
interface Ethernet0/0
port link-mode route
pppoe-client dial-bundle-number 1
# 建立 PPPoE 会话，并且指定该会话所对应的 Dialer bundle
dialer rule 1 ip permit
# 配置触发条件
```

### 查看 PPPoE 协商阶段状态

查看 PPPoE 协商阶段是否完成，正常状态下 State 应该为 PPPUP 状态，PPPUP 状态为 session 阶段 PPP 协商已经通过，可以传输网络数据；PADI 则为发出 PADI 报文等待 PADO 报文，PADR 则为发出 PADR 报文等待 PADS 报文，PPPNCP 则为开始 session 阶段的 PPP 协商。

如果会话状态是 PPPUP，则通过了 PPPoE Discovery 阶段。

**命令：** `display pppoe-client session summary`

例如：通过命令查看 pppoe-client 会话状态为 PPPUP。

```bash
<H3C>display pppoe-client session summary
PPPoE Client Session Information
Interface: Eth0/0
Bundle: Dialer1
Remote MAC: 0023-892f-2780
Local MAC: 5866-b883-0f70
State: PPPUP
```

### 查看 PPPoE 发现阶段交互流程

PPPoE 建立过程分为两个阶段：发现阶段（PPPoE Discovery Stage）和会话阶段（PPPoE Session Stage），发现阶段获得对端的以太网 MAC 地址并确定一个 PPPoE SESSION_ID。

- PADI 用于 PPPoE client 发现 PPPoE 集中服务器，在 VLAN 内广播
- PADO 用于集中服务器响应收到的 PADI 报文，声明自己能为 PPPoE client 提供服务
- PADR 用于 PPPoE client 向特定的 PPPoE 集中服务器申请 PPPoE 服务
- PADS 用于集中服务器响应收到的 PADR 报文，并为此 PPP 会话分配资源
- PADT 用于终止 PPPoE 会话

可通过 `debugging pppoe-client all interface interface-type interface-number` 命令查看报文交互过程。

**命令：** `debugging pppoe-client all`

例如：查看 Ethernet0/0 接口对应的 Discovery 阶段报文交互过程如下。

```bash
Jan 2 06:19:04:267 2007 client PPPOEC/7/debugging:
Ethernet0/0: PPPoE client out discovery packet (PADI), Len 30
Jan 2 06:19:04:270 2007 client PPPOEC/7/debugging:
Ethernet0/0: PPPoE client in discovery packet (PADO), Len 60
Jan 2 06:19:04:270 2007 client PPPOEC/7/debugging:
Ethernet0/0: PPPoE client out discovery packet (PADR), Len 52
Jan 2 06:19:05:738 2007 client PPPOEC/7/debugging:
Ethernet0/0: PPPoE client in discovery packet (PADS), Len 60, Session ID 1234
Jan 2 06:19:05:838 2007 client PPPOEC/7/debugging: Ethernet0/0: Created Dialer1:0 for PPPoE session
```

如果报文有丢失或异常，请检查网络情况及 PPPoE client 与 PPPoE server 协商参数是否匹配。

### 查看 PPPoE 会话阶段交互流程

PPPoE 建立过程分为两个阶段：发现阶段（PPPoE Discovery Stage）和会话阶段（PPPoE Session Stage）。

会话阶段包括：LCP 阶段、Authentication 阶段（可选）、NCP 阶段（IPCP）。

PPP 业务阶段（3），排查方法可参考 PPP 模块部分。

---

## 4.1.4 PPPoE Server 故障排查

PPPoE（Point-to-Point Protocol over Ethernet，在以太网上承载 PPP 协议）的提出，解决了 PPP 无法应用于以太网的问题，是对 PPP 的扩展。PPPoE 将 PPP 报文封装在以太网帧之内，在以太网上提供点对点的连接。

PPPoE 可以通过一个远端接入设备为以太网上的主机提供互联网接入服务，并对接入的每个主机实现控制、计费功能。

PPPoE 定位故障的思路是：先查看配置是否正确，之后根据协商阶段查看 Discovery 发现阶段与 Session 会话阶段报文交互过程。

### 检查 PPPoE Server 配置

检查配置应关注，VT 接口下 PPP 认证方式与用户名密码是否匹配，以太网接口上启用 PPPoE 协议，绑定虚拟模板接口。

**命令：** `display current-configuration`

例如：在设备上通过 `display current-configuration` 查看配置是否正确。

```bash
[H3C]display current-configuration
local-user 123
# 验证用户名与密码，与 PPPoE client 使用用户名一致
password cipher $c$3$pmkkgfgT7KmsLGvkXwVJsod0WvsN4Q==
service-type ppp
interface Ethernet0/0
port link-mode route
pppoe-server bind Virtual-Template1
# 在以太网接口上启用 PPPoE 协议，将该以太网接口与指定的虚拟模板接口绑定
interface Virtual-Template1
ppp authentication-mode pap
# 认证方式为 PAP
remote address 1.1.1.2
# 给 client 分配地址 1.1.1.2
ip address 1.1.1.1 255.0.0.0
# 网关地址
```

### 查看 PPPoE 发现阶段交互流程

PPPoE 建立过程分为两个阶段：发现阶段（PPPoE Discovery Stage）和会话阶段（PPPoE Session Stage）。

发现阶段获得对端的以太网 MAC 地址并确定一个 PPPoE SESSION_ID。

- PADI 用于 PPPoE client 发现 PPPoE 集中服务器，在 VLAN 内广播
- PADO 用于集中服务器响应收到的 PADI 报文，声明自己能为 PPPoE client 提供服务
- PADR 用于 PPPoE client 向特定的 PPPoE 集中服务器申请 PPPoE 服务
- PADS 用于集中服务器响应收到的 PADR 报文，并为此 PPP 会话分配资源
- PADT 用于终止 PPPoE 会话

可通过 `debugging pppoe-server all interface interface-type interface-number` 命令查看报文交互过程。

**命令：** `debugging pppoe-server all interface interface-type interface-number`

例如：查看 Ethernet0/0 接口对应的 Discovery 阶段报文交互过程如下。

```bash
Jan 27 07:56:230 2007 Server PPPOE/7/debug:
2007-1-27:7:56.228: PPPoE Event: Ethernet0/0, IN PADI packet
Jan 27 07:56:230 2007 Server PPPOE/7/debug:
2007-1-27:7:56.229: PPPoE Event: Ethernet0/0, OUT PADO packet
Jan 27 07:56:239 2007 Server PPPOE/7/debug:
2007-1-27:7:56.231: PPPoE Event: Ethernet0/0, IN PADR packet
Jan 27 07:56:240 2007 Server PPPOE/7/debug:
2007-1-27:7:56.237: PPPoE Event: Ethernet0/0, OUT PADS packet (Session Virtual-Template1:0), UID 1234
```

如果报文有丢失或异常，请检查网络情况及 PPPoE server 与 PPPoE client 协商参数是否匹配。

### 查看 PPPoE 会话阶段交互流程

PPPoE 建立过程分为两个阶段：发现阶段（PPPoE Discovery Stage）和会话阶段（PPPoE Session Stage）。

会话阶段包括：LCP 阶段、Authentication 阶段（可选）、NCP 阶段（IPCP）。

PPP 业务阶段（3），排查方法可参考 PPP 模块部分。

---

## 4.1.5 CR16000 和 SR8800 产品硬件故障排查

CR16000/SR8800 产品出现硬件故障问题，首先应判断是哪一个模块出现故障：风扇模块、电源模块或单板。

### 风扇模块

CR16000/SR8800 产品出现风扇故障问题，首先应确认风扇指示灯状态，命令行确认风扇运行状态，设备是否打印风扇异常信息。

如果风扇指示灯异常，或者命令行查询到风扇运行状态异常，或者设备打印风扇异常信息，则需要拔出风扇框观察确认是否有风扇停转，插拔风扇框并交叉验证确认故障，是否有异物。

确认故障后需要收集相关故障信息反馈 400。

如果风扇指示灯正常，命令行查询风扇运行状态正常，同时设备没有打印风扇异常信息，则说明风扇运行正常，如果还有疑问，请拨打 400-810-0504 寻求帮助。

**命令：** `display fan`

CR16000：
```bash
<H3C>display fan
Fan-tray state on chassis 0:
tray  State: Normal
```

SR8800：
```bash
<H3C>display fan
Fan State: Normal
```

设备打印风扇异常信息如下：
```bash
%Jun 26 10:12:24:805 2013 H3C DEV/3/FAN_ABSENT: Fan 1 is absent
%Jun 26 10:12:32:805 2013 H3C DEV/2/DRV_DEV_FAN_CHANGE: Fan communication State changed: Fan 1 changed to fault.
%Jun 26 10:12:42:405 2013 H3C DEV/2/FAN_FAILED: Fan 1 failed
```

### 电源模块

CR16000/SR8800 产品电源问题的主要排查思路是：先查看电源指示灯，根据电源指示灯排查确认是外部供电问题，还是电源模块自身的问题；如果电源指示灯正常，但是命令行显示电源状态异常，或者设备打印电源状态异常的日志信息，请直接收集故障信息反馈 400。

**查看电源指示灯：**

电源监控模块指示灯说明（CR16000 特有）：

| 指示灯名称 | 颜色 | 状态 | 含义 |
|-----------|------|------|------|
| RUN | 绿色 | 灯常亮 | 表示电源监控模块正常工作 |
| RUN | 绿色 | 灯闪烁 | 表示电源监控模块有故障，包括但不限于以下 4 种情况：表示电源模块有故障；电源框内所有电源模块被拔出；电源框开关关闭；电源模块在位，但未接入交流电 |
| MAJOR | 红色 | 灯常亮 | 表示插拔电源模块 |
| MAJOR | 红色 | 灯灭 | 表示电源模块正常工作 |
| MINOR | 黄色 | 灯常亮 | 保留 |
| MINOR | 黄色 | 灯灭 | 保留 |

电源模块指示灯说明：

| 指示灯名称 | 颜色 | 状态 | 含义 |
|-----------|------|------|------|
| 电源输入指示灯 | 绿色 | 灯常亮 | 表示电源输入正常 |
| 电源输入指示灯 | 绿色 | 灯灭 | 表示电源没有输入 |
| 电源输入指示灯 | 绿色 | 灯闪烁 | 表示电源输入超过最大范围 |
| 电源输出指示灯 | 绿色 | 灯常亮 | 表示电源模块输出正常 |
| 电源输出指示灯 | 绿色 | 灯灭 | 表示电源模块没有输出 |
| 电源输出指示灯 | 绿色 | 灯闪烁 | 表示电源模块输出过载 |
| 过温告警指示灯 | 黄色 | 灯常亮 | 表示电源模块过温告警 |
| 过温告警指示灯 | 黄色 | 灯灭 | 表示电源模块正常工作 |
| 电源故障指示灯 | 红色 | 灯常亮 | 表示电源模块出现故障 |
| 电源故障指示灯 | 红色 | 灯灭 | 表示电源模块正常工作 |

**查看电源状态命令：** `display power-supply verbose`

例如：通过命令查看电源状态

```bash
<H3C>display power-supply verbose
PSU Status
ID Status Input-Err Output-Err High-Temperature FanErr Closed Current-Limit
1/1 Normal
1/2 Absent
1/3 Normal
1/4 Absent
1/5 Failed
1/6 Absent
```

### 单板无法注册问题

CR16000/SR8800 产品出现单板无法注册问题，首先应判断是开局故障，还是运行中出现的故障。

如果是开局故障，则需要依次排查以下方面：
1. 单板是否误插和单板是否插紧
2. 单板与设备运行的软件版本是否配套，如果是备用主控板，其版本是否和主用主控板一致
3. 设备系统模式设置是否正确和系统电源功率是否满足要求
4. 最后使用交叉验证的方式初步判定是否存在硬件故障

如果是运行中出现故障，请直接收集故障信息并联系 400。

**查看单板状态命令：** `display device`

**查看系统工作模式命令：** `display system working-mode`

例如：通过命令查看，可以确认 SR8800 的系统工作模式为 Hybrid。

```bash
<H3C>display system working-mode
Current system working-mode: Hybrid
Working Mode: System restart: Hybrid
Notice: Changing working mode take effect only after system restart
```

---

## 4.1.6 MSR 硬件故障排查

常见 MSR（V5/V7）设备硬件故障可分为环境及单板硬件状态异常、板卡接口检测故障、以太网接口故障，其中常遇到的硬件问题为：单板无法注册、设备无法启动、接口无法 Up 等。

硬件故障排查一般方法为：最常用的判断方法为替代测试，如果无法进行替代测试可根据对应设备调试命令，或设备异常堆栈信息进行判断。

### 环境状态

各传感器温度应处于低温告警门限与一般级高温告警门限之间，如出现异常温度告警，比如：-1°C、255°C 等，一般是软件版本问题或温度传感器硬件故障，需要更换硬件。

**命令：** `display environment`

例如：通过命令查看设备当前温度为 41°C，设置温度告警上限 60°C，下限 5°C。

```bash
<H3C>display environment
System Temperature information (degree centigrade)
SlotNo Temperature Lower limit Upper limit
0      41         5           60
```

### 风扇状况

风扇状态应该显示 Normal，如出现 Fault 或 Absent，建议重新插拔风扇尝试，如果运行时间较长，建议先除尘，并升级最新版本；如除尘、升级版本后问题仍然存在，建议更换风扇，对于集中式设备，建议更换设备。

**命令：** `display fan`

例如：通过命令显示设备风扇工作状态为 Normal。

```bash
<H3C>display fan
Fan State: Normal
```

### 电源状况

电源应该显示 Normal，如出现 Fault 或 Absent，建议重新插拔后尝试，并升级最新版本。如果设备只有一个电源，且该电源状态不正常，则更换电源或者设备；如果有多个电源，还需确认电源是否在位或者没有开电，如果确认电源在位、开电，电源状态异常，则更换电源或者设备。

**命令：** `display power`

例如：显示设备电源的状况为 Normal。

```bash
<H3C>display power
Power Supply State: Normal
```

### 板卡运行状况

所有业务板应该是 Normal，如果显示为 Fault 则表示该槽位单板出错，不能正常启动。出现无法注册问题，或者不能识别问题，建议官网查看"接口模块手册-03-附录 B 适配关系章节"与版本手册，如果槽位适配关系与版本都符合要求，可进行替代测试，确认单板或主机故障，进行更换。

**命令：** `display device`

例如：设备板卡 SIC-IFEA 在槽位 1 上状态为 Normal。

```bash
<H3C>display device
Slot No. Board Type           Status   Max Ports
0        MSR20-21-RPU Board   Normal
1        SIC-IFEA              Normal
```

### 设备无法启动

常见有下面 5 种情况：

1. 找不到启动文件，请首先确认启动文件本身是否正常，可通过 md5 检验确认，之后判断是否为 Flash 故障导致启动文件异常，此时可以按 Ctrl+F 键格式化 Flash 后重新加载启动文件，如格式化过程报错，需要更换主机板。

2. 电源无法上电（无反应），建议更换电源插座、电源线、PC 电源线测试，如果确认都正常，可以进行主机更换。

3. 开机，按照提示进行内存检测，如果检测失败，进行主机更换。

4. 如果启动过程中出现"卡死"现象，建议拔掉所有单板后重启尝试，确认是主机故障还是板卡故障引起启动异常，进行相应设备硬件更换。

5. 启动过程中，打印乱码或异常信息，可进行设备硬件更换。

### 板卡接口故障检测

对于板卡接口类故障常见排查方法为接口替代测试，如果无法进行替代测试，可以进行接口打环测试，通过命令配置本地环回，查看接口是否检测到环回，是否 Up，如果打环测试失败，接口无法 Up，建议更换板卡；对于常用的 E1 与串口板卡，打环测试同时，还需要观察接口收发包是否一致（需先清空接口计数），打环成功，收发包不一致，也需要硬件更换，具体判断方法可参考接口类型对应的章节。

### 以太网接口故障检测

首先使用 `loopback internal` 命令对于以太网接口做环回测试，如果检测失败，直接进行模块或主机更换，如果成功，接口使用中无法 Up，可以用 PC 加交叉网线与该接口连接测试，在保证网线与 PC 正常情况下，接口依然无法 Up，可进行板卡或硬件更换。

固定三层接口：
```bash
[H3C-Ethernet0/0]loopback internal
%Jan 00:12:27:584 2007 client IFNET/3/LINK_UPDOWN: Ethernet0/0 link status is Up
%Jan 00:12:27:585 2007 client IFNET/5/LINEPROTO_UPDOWN: Line protocol on the interface Ethernet0/0 is Up
[H3C-Ethernet0/0]display interface
Ethernet0/0
Current State: Up
Line protocol current state: Up
Description: Ethernet0/0 Interface
The Maximum Transmit Unit is 1500
Internet protocol processing disabled
(3) Packet Frame Type: PKTFMT_ETHNT_2
Hardware Address: 5866-b883-0f70
```

二层交换接口：
```bash
[H3C-Ethernet0/4]loopback internal
%Jan 00:13:05:645 2007 client IFNET/3/LINK_UPDOWN: Ethernet0/4 link status is Up
[H3C-Ethernet0/4]display interface
Ethernet0/4
Current State: Up
(3) Packet Frame Type: PKTFMT_ETHNT_2
Hardware Address: 5866-b883-0f72
Description: Ethernet0/4 Interface
Loopback is set internal
```

---

## 4.1.7 MPLS L2VPN 故障排查

由于使用 LDP 方式的 MPLS L2VPN 通过 LDP 协议向远端通告本端 PW 标签等信息，报文转发是基于外层 LSP 的，而 LSP 是依附于路由的。所以定位故障的思路是：先查路由，再查 LSP/LDP，最后排查 MPLS L2VPN 配置。

### 查看公网路由

确认是否在公网 LSP 途径的所有设备上都存在两端 PE 的 loopback 地址精确路由。

**命令：** `display ip routing-table`

例如：通过命令查看，可以确认存在对端 PE 的 Loopback 路由 2.2.2.2/32，下一跳是 10.0.0.2。

```bash
[H3C]display ip routing-table
Destinations: 14       Routes: 14
Destination/Mask    Proto  Pre  Cost   NextHop      Interface
2.2.2.2/32          OSPF   10   0      10.0.0.2     GE0/1
```

### 确认和修改公网 IGP 配置

是否通过公网 IGP 将 PE 的 loopback 地址的路由发布出去。

例如：公网 IGP 使用 OSPF，查看 OSPF 配置，可以确认已将 PE 的 loopback 地址 1.1.1.1 发布进公网 OSPF 内。

```bash
[H3C]display current-configuration configuration ospf
ospf 1
area 0.0.0.0
network 1.1.1.1 0.0.0.0
network 10.0.0.0 0.0.0.0
```

### 查看公网 LSP 信息及 LDP 状态

查看整个 LSP 上的所有设备是否已经为两个 PE 的 loopback 地址正确的分配了公网标签，以及 LDP Peer 是否正常。

**命令：** `display mpls lsp`
**命令：** `display mpls ldp peer`

例如：通过命令查看，到达 2.2.2.2 的 LSP 已经建立，LSP 状态正常。

```bash
[H3C]display mpls lsp
FEC              Proto   In/Out Label  Interface / Out NHLFE
1.1.1.1/32       LDP     31/Null       Local
2.2.2.2/32       LDP     -13/1151      GE0/1 10.0.0.2
```

```bash
[H3C]display mpls ldp peer
Total number of peers: 1
Peer LDP ID        State      Role     MD5    KA Sent/Rcvd
2.2.2.2:65535     Operational Passive  Off    34/33
```

### 确认和修改公网 MPLS 配置

在沿途所有设备上全局开启 MPLS 和 MPLS LDP，并在接口下配置 MPLS 和 MPLS LDP。

例如：本端 PE 上设置 `mpls lsr-id 1.1.1.1`，全局和接口下开启 MPLS 和 MPLS LDP。

```bash
[H3C]display current-configuration configuration mpls
mpls lsr-id 1.1.1.1
mpls
mpls ldp
interface GigabitEthernet0/0
mpls enable
mpls ldp enable
```

### 查看 PW 状态

分别查看两端 PE 路由器的 PW 状态以及 AC 接口状态是否 Up。

**命令：** `display l2vpn xconnect-group verbose`

例如：通过命令查看，查看与 Peer 2.2.2.2 建立的 PW-ID 为 123 的 PW 状态正常。AC 接口状态正常。

```bash
[H3C]display l2vpn xconnect-group verbose
Xconnect-group Name: 1
Connection Name: 1
Connection ID: 0x1
State: Up
MTU: 1500
LDP PWs:
Peer      PW ID  Link ID  State
2.2.2.2   123    1        Up
ACs:
AC    Link ID  State
GE0/1  1        Up
```

### 确认和修改 PW 配置

查看配置交叉连接组，绑定 AC 接口，并指定对端 loopback 地址，而且 pw-id 以及 pw-class 必须一致。

例如：创建交叉连接组 1，创建交叉连接 1，在交叉连接下绑定 AC 接口，并指定与对端（2.2.2.2）建立 PW，PW-ID 为 123，同时引用 PW 模板 1。

```bash
[H3C]display current-configuration configuration xconnect-group
xconnect-group 1
connection 1
interface GigabitEthernet0/1
peer 2.2.2.2 pw-id 123 pw-class 1
```

查看 PW 类配置：

```bash
[H3C]display l2vpn pw-class
Total number of PW Class: 1
PW Class Name: 1
PW Type: Ethernet
Control Word: Enabled
Control-Word: BFD
VCCV CC: BFD
```
